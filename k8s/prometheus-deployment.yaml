apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-deployment
  labels:
    app: prometheus-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-app-deployment
  template:
    metadata:
      labels:
        app: prometheus-app-deployment
    spec:
      serviceAccountName: prometheus-service-account
      containers:
        - name: prometheus-app-deployment
          image: prom/prometheus:v2.53.2
          resources:
            limits:
              cpu: '0.125'
              memory: '640Mi'
            requests:
              cpu: '0.0625'
              memory: '128Mi'
          args:
            - '--config.file=/etc/prometheus/prometheus.yml'
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: tls-volume
              mountPath: /etc/prometheus/cert
              readOnly: true
            - name: prometheus-config
              mountPath: /etc/prometheus
      volumes:
        - name: tls-volume
          secret:
            secretName: common-ca-tls-secret
        - name: prometheus-config
          configMap:
            name: prometheus-config
