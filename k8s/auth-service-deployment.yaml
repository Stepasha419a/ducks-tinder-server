apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service-deployment
  labels:
    app: auth-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-app-service-deployment
  template:
    metadata:
      labels:
        app: auth-app-service-deployment
    spec:
