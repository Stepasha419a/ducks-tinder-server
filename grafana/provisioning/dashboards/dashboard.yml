apiVersion: 1

providers:
  - name: 'Prometheus'
    type: file
    updateIntervalSeconds: 15
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true
